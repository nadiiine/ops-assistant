# Example only. Do not put a real API key here.
# Create the live secret with:
#   kubectl -n ops-assistant create secret generic ops-assistant \
#     --from-literal=OPENAI_API_KEY='your-key'
apiVersion: v1
kind: Secret
metadata:
  name: ops-assistant
  namespace: ops-assistant
type: Opaque
stringData:
  OPENAI_API_KEY: "replace-me"
