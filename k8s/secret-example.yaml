# Example only. Bedrock uses the node instance IAM role (no API key secret).
# The previous GEMINI_API_KEY secret is no longer required for the Bedrock path.
# If you still have an old secret, you may delete it after switching providers:
#   kubectl -n ops-assistant delete secret ops-assistant --ignore-not-found
apiVersion: v1
kind: Secret
metadata:
  name: ops-assistant
  namespace: ops-assistant
type: Opaque
stringData: {}
